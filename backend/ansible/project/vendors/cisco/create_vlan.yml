---
- name: Create VLAN on Cisco device
  hosts: all
  gather_facts: no

  tasks:
    - name: Ensure VLAN exists with name
      cisco.ios.ios_config:
        parents: "vlan {{ vlan_id }}"
        lines:
          - name {{ vlan_name }}
      register: result
    - name: Save running config
      cisco.ios.ios_config:
        save_when: always
