# GNS3 inventory — credentials are passed dynamically per-request from the device registry.
# Add static hosts here if needed for manual testing.
#
# Example:
# [cisco]
# router1 ansible_host=192.168.56.101 ansible_user=cisco ansible_password=cisco ansible_network_os=ios ansible_connection=network_cli
