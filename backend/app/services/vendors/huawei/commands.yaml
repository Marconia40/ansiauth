# Comandos CLI de Huawei VRP por operación. Editar acá para agregar una
# variación descubierta contra un device/modelo real -- no hace falta tocar
# driver.py ni reconstruir el playbook. Cada operación tiene un "primary" y,
# opcionalmente, "alternatives": cada alternativa lleva "triggered_by_error"
# (regex contra stderr+stdout del intento primario) -- si matchea, se
# reintenta una vez con esa alternativa antes de devolver el error real.
# "alternatives" es una lista -- se pueden apilar varias para la misma
# operación, se prueban en orden hasta que una matchee.
#
# "block" es la única forma que usa este vendor (ansible.netcommon.cli_command,
# un solo comando/bloque multi-línea) -- las líneas se unen con "\n" y se
# mandan como una sesión completa (system-view/.../commit/quit según haga
# falta). "y" como línea propia contesta un prompt [Y/N] (ver reset_port).
#
# Placeholders disponibles por operación: ver el método correspondiente en
# driver.py (arma el dict "vars" antes de llamar a _aplicar_desde_template()).
#
# Trigger _NO_COMMIT (reutilizado en varias operaciones abajo): confirmado
# contra un device real de lab (prompt "[rou-aulascomunes-a1]", otra
# familia de plataforma VRP que no usa candidate-config/commit) --
# "commit" mismo sale rechazado con:
#   [prompt]commit
#                ^
#   Error: Unrecognized command found at '^' position.
# El regex ancla específicamente en que la palabra ecoada antes del "^" sea
# "commit" -- no un "Unrecognized command" genérico, que podría venir de
# cualquier otra línea del bloque fallando por una razón totalmente
# distinta (ya nos pasó con PoE/storm-control en el CE12800, que no tiene
# nada que ver con esto). Probado: no matchea esos otros casos.

create_vlan:
  primary:
    block:
      - "system-view"
      - "vlan {vlan_id}"
      - "description {name}"
      - "quit"
      - "commit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "vlan {vlan_id}"
        - "description {name}"
        - "quit"
        - "quit"

delete_vlan:
  primary:
    block:
      - "system-view"
      - "undo vlan {vlan_id}"
      - "commit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "undo vlan {vlan_id}"
        - "quit"

update_vlan:
  primary:
    block:
      - "system-view"
      - "vlan {vlan_id}"
      - "description {name}"
      - "quit"
      - "commit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "vlan {vlan_id}"
        - "description {name}"
        - "quit"
        - "quit"

save_config:
  primary:
    block: ["save force"]

list_vlans:
  primary:
    commands: ["display vlan"]

list_ports:
  primary:
    commands:
      - "display interface brief"
      - "display interface description"
      - "display port vlan"

update_port_description:
  set:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "description {description}"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "description {description}"
          - "quit"
          - "quit"
  clear:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "undo description"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "undo description"
          - "quit"
          - "quit"

set_port_admin_state:
  enabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "undo shutdown"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "undo shutdown"
          - "quit"
          - "quit"
  disabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "shutdown"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "shutdown"
          - "quit"
          - "quit"

set_port_access_vlan:
  primary:
    block:
      - "system-view"
      - "interface {interface}"
      - "port default vlan {vlan_id}"
      - "commit"
      - "quit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface {interface}"
        - "port default vlan {vlan_id}"
        - "quit"
        - "quit"

set_trunk_pvid_vlan:
  primary:
    block:
      - "system-view"
      - "interface {interface}"
      - "port trunk pvid vlan {vlan_id}"
      - "commit"
      - "quit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface {interface}"
        - "port trunk pvid vlan {vlan_id}"
        - "quit"
        - "quit"

set_trunk_allowed_vlans:
  primary:
    block:
      - "system-view"
      - "interface {interface}"
      - "undo port trunk allow-pass vlan all"
      - "port trunk allow-pass vlan {allowed_vlans}"
      - "commit"
      - "quit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface {interface}"
        - "undo port trunk allow-pass vlan all"
        - "port trunk allow-pass vlan {allowed_vlans}"
        - "quit"
        - "quit"

set_port_poe:
  enabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "poe enable"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "poe enable"
          - "quit"
          - "quit"
  disabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "poe disable"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "poe disable"
          - "quit"
          - "quit"

# Sintaxis confirmada contra un device real (no el CE12800 de lab): usa
# min-rate/max-rate por tipo de tráfico, no un único "level" como Cisco --
# {threshold} se sustituye en las 4 posiciones numéricas (broadcast +
# multicast, min y max iguales). "storm control action block" +
# "storm control enable trap" son fijas, sin placeholder. Si el equipo
# soporta la línea de broadcast, soporta multicast con el mismo comando --
# no es un eje de variación aparte (descartado, no es "algunos Huawei no
# soportan multicast" como se pensó en un principio).
#
# 2 causas reales conocidas, correlacionadas entre sí (confirmado en
# campo): los Huawei que escriben el comando SIN guión ("storm control",
# como el primary de acá abajo) usan candidate-config con "commit"; los
# que lo escriben CON guión ("storm-control") aplican directo, sin
# "commit" -- mismo criterio que el resto de este archivo. 2 alternativas:
#   - "storm control" entero rechazado (nada se aplicó, como el caso de
#     encapsulation de Cisco) -- alternativa: mismo bloque completo, pero
#     con guión y sin "commit".
#   - Solo "commit" rechazado (las líneas de antes ya se aplicaron,
#     re-mandarlas es redundante pero no incorrecto -- se hace igual por
#     seguridad: si además de "commit" hubiera fallado otra línea por una
#     razón real y separada, achicar la alternativa a un simple "quit"
#     taparía ese error en vez de dejarlo salir de nuevo) -- alternativa:
#     mismo bloque completo, misma sintaxis del primary (espacio), sin
#     "commit".
# Cuadrante sin confirmar todavía: guión CON commit -- no se agrega a
# ciegas, esperar un caso real.
set_storm_control:
  enabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "storm control broadcast min-rate percent {threshold} max-rate percent {threshold}"
        - "storm control multicast min-rate percent {threshold} max-rate percent {threshold}"
        - "storm control action block"
        - "storm control enable trap"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: 'storm control[^\n]*\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "storm-control broadcast min-rate percent {threshold} max-rate percent {threshold}"
          - "storm-control multicast min-rate percent {threshold} max-rate percent {threshold}"
          - "storm-control action block"
          - "storm-control enable trap"
          - "quit"
          - "quit"
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        # A diferencia de la alternativa de arriba, acá lo único que falló
        # fue "commit" -- las líneas de storm control (con espacio, lo que
        # manda el primary) ya se aplicaron bien. Reusa la misma sintaxis
        # del primary, no cambia a guión sin motivo.
        block:
          - "system-view"
          - "interface {interface}"
          - "storm control broadcast min-rate percent {threshold} max-rate percent {threshold}"
          - "storm control multicast min-rate percent {threshold} max-rate percent {threshold}"
          - "storm control action block"
          - "storm control enable trap"
          - "quit"
          - "quit"
  disabled:
    primary:
      block:
        - "system-view"
        - "interface {interface}"
        - "undo storm control broadcast"
        - "undo storm control multicast"
        - "undo storm control action"
        - "undo storm control enable trap"
        - "commit"
        - "quit"
        - "quit"
    alternatives:
      - triggered_by_error: 'storm control[^\n]*\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface {interface}"
          - "undo storm-control broadcast"
          - "undo storm-control multicast"
          - "undo storm-control action"
          - "undo storm-control enable trap"
          - "quit"
          - "quit"
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        # Mismo criterio que en "enabled": solo falló "commit", reusa la
        # sintaxis del primary (espacio), no cambia a guión.
        block:
          - "system-view"
          - "interface {interface}"
          - "undo storm control broadcast"
          - "undo storm control multicast"
          - "undo storm control action"
          - "undo storm control enable trap"
          - "quit"
          - "quit"

reset_port:
  primary:
    # "clear configuration interface" pide confirmación interactiva
    # ("Warning: ... Continue? [Y/N]") y se aplica de inmediato -- no lleva
    # "commit" después. Confirmado contra el device real: con "commit" en
    # la línea siguiente, el device lo interpretaba como respuesta
    # inválida al prompt Y/N y quedaba reintentando hasta timeout.
    block:
      - "system-view"
      - "clear configuration interface {interface}"
      - "y"
      - "quit"

set_access_mode:
  primary:
    block:
      - "system-view"
      - "interface {interface}"
      - "port link-type access"
      - "port default vlan {vlan_id}"
      - "commit"
      - "quit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface {interface}"
        - "port link-type access"
        - "port default vlan {vlan_id}"
        - "quit"
        - "quit"

set_trunk_mode:
  primary:
    block:
      - "system-view"
      - "interface {interface}"
      - "port link-type trunk"
      - "port trunk pvid vlan {native_vlan}"
      - "undo port trunk allow-pass vlan all"
      - "port trunk allow-pass vlan {allowed_vlans}"
      - "commit"
      - "quit"
      - "quit"
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface {interface}"
        - "port link-type trunk"
        - "port trunk pvid vlan {native_vlan}"
        - "undo port trunk allow-pass vlan all"
        - "port trunk allow-pass vlan {allowed_vlans}"
        - "quit"
        - "quit"

# ── SVI, RF-INTERV-* ─────────────────────────────────
# "interface Vlanif{vlan_id}". Reusa el mismo trigger _NO_COMMIT que
# VLAN/puerto en cada operación -- misma plataforma, mismo comportamiento
# de sesión CLI, no hay motivo para que solo algunas operaciones lo
# necesiten.

create_svi:
  primary:
    block: ["system-view", "interface Vlanif{vlan_id}", "undo shutdown", "commit", "quit", "quit"]
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface Vlanif{vlan_id}"
        - "undo shutdown"
        - "quit"
        - "quit"

delete_svi:
  primary:
    block: ["system-view", "undo interface Vlanif{vlan_id}", "commit", "quit"]
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "undo interface Vlanif{vlan_id}"
        - "quit"

set_svi_admin_state:
  enabled:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo shutdown", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo shutdown"
          - "quit"
          - "quit"
  disabled:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "shutdown", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "shutdown"
          - "quit"
          - "quit"

set_svi_description:
  set:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "description {description}", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "description {description}"
          - "quit"
          - "quit"
  clear:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo description", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo description"
          - "quit"
          - "quit"

# Confirmado contra el device real de lab (running-config visto esta
# sesión: "interface Vlanif10 / ip address 10.10.10.10 255.255.255.0").
# {ipv4_addr}/{ipv4_mask} ya vienen separados (máscara punteada) -- la
# conversión CIDR->máscara es cómputo real, la hace el driver en Python.
set_svi_ipv4:
  set:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "ip address {ipv4_addr} {ipv4_mask}", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "ip address {ipv4_addr} {ipv4_mask}"
          - "quit"
          - "quit"
  clear:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo ip address", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo ip address"
          - "quit"
          - "quit"

# RF-INTERV-03: IP secundaria -- VRP llama "sub" a esto, confirmado contra
# config real de producción (ver ejemplo de Vlanif832 en la nota de
# set_svi_ipv6 más abajo -- misma interfaz, "ip address ... sub").
# Requiere primaria existente (lo valida SVI antes de llegar
# acá).
set_svi_ipv4_secondary:
  set:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "ip address {ipv4_addr} {ipv4_mask} sub", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "ip address {ipv4_addr} {ipv4_mask} sub"
          - "quit"
          - "quit"
  clear:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo ip address {ipv4_addr} {ipv4_mask} sub", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo ip address {ipv4_addr} {ipv4_mask} sub"
          - "quit"
          - "quit"

# Confirmado contra config real de producción (interface Vlanif832):
#   ipv6 enable
#   ip address 200.16.26.129 255.255.255.128
#   ip address 172.23.112.1 255.255.255.0 sub
#   ipv6 address 2801:120:832::1/64
# "ipv6 enable" hace falta antes de poder asignar la dirección (a
# diferencia de Cisco, que solo necesita "ipv6 unicast-routing" global) --
# pero a diferencia del IPv4 de esta misma plataforma, ipv6 SÍ va en CIDR
# de un tirón, igual que Cisco (no separado addr+prefix-length).
set_svi_ipv6:
  set:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "ipv6 enable", "ipv6 address {ipv6_address}", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "ipv6 enable"
          - "ipv6 address {ipv6_address}"
          - "quit"
          - "quit"
  clear:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo ipv6 address", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo ipv6 address"
          - "quit"
          - "quit"

# Confirmado contra config real de producción -- el orden de
# "traffic-filter" cambia según si la ACL es numerada o con nombre:
#   traffic-filter inbound acl 3002                           (numerada)
#   traffic-filter acl servers-admin-dc2-vlan830 inbound       (con nombre)
# driver.py decide "set_numeric" vs "set_named" según acl_name.isdigit().
# Asigna una ACL que ya existe en el device, no la crea (RF-GLOBAL-04,
# aparte).
set_svi_acl:
  set_numeric:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "traffic-filter {direction}bound acl {acl_name}", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "traffic-filter {direction}bound acl {acl_name}"
          - "quit"
          - "quit"
  set_named:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "traffic-filter acl {acl_name} {direction}bound", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "traffic-filter acl {acl_name} {direction}bound"
          - "quit"
          - "quit"
  clear:
    primary:
      block: ["system-view", "interface Vlanif{vlan_id}", "undo traffic-filter {direction}bound", "commit", "quit", "quit"]
    alternatives:
      - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
        block:
          - "system-view"
          - "interface Vlanif{vlan_id}"
          - "undo traffic-filter {direction}bound"
          - "quit"
          - "quit"

# {servers} es la lista completa de relay servers (SVI arma el
# full-replace antes de llegar acá -- RF-INTERV-05 es incremental a nivel
# API, full-replace a nivel CLI). Hay 2 formas reales confirmadas, y varían
# por plataforma -- primary intenta la forma directa, alternatives cubre
# tanto el "sin commit" ya conocido como la forma por "server group" para
# las plataformas que rechazan la directa.
#
# FORMA DIRECTA (primary) -- "dhcp select relay" (una vez, solo si la
# lista no está vacía) + "dhcp relay server-ip {ip}" por cada elemento,
# después del "undo dhcp select relay" fijo que limpia cualquier config
# previa. Confirmada contra config real de un device de producción.
#
# FORMA POR "SERVER GROUP" (2da alternativa) -- confirmada contra el
# MISMO device de producción (coexisten, se elige una u otra por
# interfaz) Y contra el CE12800 de lab (donde la forma directa NO existe
# -- "dhcp relay ?" ahí no lista "server-ip", pero "dhcp relay server
# group" sí). El grupo es un objeto GLOBAL (fuera del contexto de
# interfaz) con nombre propio -- acá se genera uno determinístico por
# VLAN ("ansiauth-vlan{vlan_id}") para no necesitar estado extra ni
# compartir grupos entre interfaces (que es para lo que Huawei pensó esta
# forma, pero no encaja con el modelo por-interfaz de RF-INTERV-05):
#   dhcp relay server group ansiauth-vlan{vlan_id}
#    server {ip} {index}          -- {index} 0-based, "INTEGER<0-19>" confirmado (máx 20 por grupo en el CE12800)
#   interface Vlanif{vlan_id}
#    dhcp select relay
#    dhcp relay binding server group ansiauth-vlan{vlan_id}
# Full-replace real y confirmado contra el device de lab (setup -> setup
# de nuevo con otra lista -> clear, las 3 veces funcionando en secuencia):
# hace falta desvincular ANTES de poder borrar/recrear el grupo (si no,
# "Error: The DHCP server group is used on the interface N time(s)"), por
# eso el "undo dhcp relay binding server group" (sin nombre -- con nombre
# da "Too many parameters") va primero, incondicional.
#
# Limitación conocida: la 2da alternativa asume que además de rechazar la
# forma directa, el device SÍ soporta commit -- si algún device rechaza
# ambas cosas a la vez, el mecanismo de alternatives (1 solo reintento, no
# encadena) no lo cubre todavía. No confirmado que ese caso exista.
# El "undo dhcp relay binding server group" + "undo dhcp relay server
# group ansiauth-vlan{vlan_id}" va INCONDICIONAL en el primary (y su
# alternativa sin-commit), no solo en la alternativa por grupo -- bug real
# encontrado probando el clear completo (lista vacía): como el "clear" de
# la forma directa nunca manda una línea "dhcp relay server-ip" (no hay
# nada que dispare el trigger de la 2da alternativa), la forma directa
# "tiene éxito" trivialmente en un device que en realidad venía usando la
# forma por grupo -- el reintento nunca se dispara y el binding viejo
# queda colgado. Confirmado que "undo dhcp relay binding server group"
# es inofensivo cuando nunca hubo nada bindeado (probado en una interfaz
# recién creada, sin ningún dhcp relay configurado todavía).
set_svi_dhcp_relay:
  primary:
    block:
      - "system-view"
      - "interface Vlanif{vlan_id}"
      - "undo dhcp select relay"
      - "undo dhcp relay binding server group"
      - "quit"
      - "undo dhcp relay server group ansiauth-vlan{vlan_id}"
      - "interface Vlanif{vlan_id}"
    repeat:
      over: "servers"
      prefix_if_nonempty: ["dhcp select relay"]
      line: "dhcp relay server-ip {item}"
    trailer: ["commit", "quit", "quit"]
  alternatives:
    - triggered_by_error: '\]commit\r?\n\s*\^\r?\nError: Unrecognized command'
      block:
        - "system-view"
        - "interface Vlanif{vlan_id}"
        - "undo dhcp select relay"
        - "undo dhcp relay binding server group"
        - "quit"
        - "undo dhcp relay server group ansiauth-vlan{vlan_id}"
        - "interface Vlanif{vlan_id}"
      repeat:
        over: "servers"
        prefix_if_nonempty: ["dhcp select relay"]
        line: "dhcp relay server-ip {item}"
      trailer: ["quit", "quit"]
    - triggered_by_error: 'dhcp relay server-ip \S+\r?\n\s*\^\r?\nError: (?:Unrecognized|Incomplete) command'
      block:
        - "system-view"
        - "interface Vlanif{vlan_id}"
        - "undo dhcp relay binding server group"
        - "quit"
        - "undo dhcp relay server group ansiauth-vlan{vlan_id}"
      repeat:
        over: "servers"
        prefix_if_nonempty: ["dhcp relay server group ansiauth-vlan{vlan_id}"]
        line: "server {item} {index}"
        suffix_if_nonempty:
          - "quit"
          - "interface Vlanif{vlan_id}"
          - "dhcp select relay"
          - "dhcp relay binding server group ansiauth-vlan{vlan_id}"
        if_empty:
          - "interface Vlanif{vlan_id}"
          - "undo dhcp select relay"
      trailer: ["commit", "quit", "quit"]

# RF-INTERV-04's "ACL previamente creada... e identificador válido" --
# "Total nonempty ACL number is 0" confirmado contra el device real de lab
# (estado vacío). El formato de encabezado con ACLs reales configuradas
# ("Basic/Advanced ACL <num>, N rules" para numeradas, "Basic/Advanced
# Name ACL <nombre>, N rules" para con nombre -- la palabra "Name" extra
# solo aparece en ese caso) confirmado aparte contra config real de un
# device de producción (parseo en svi_parser.py).
list_acls:
  primary:
    commands:
      - "display acl all"

# get_svis NO tiene entrada acá a propósito -- confirmado
# contra el device real de lab que "display current-configuration
# interface Vlanif" sin número es rechazado ("Error: Wrong parameter
# found at '^' position"), a diferencia de Cisco. driver.py arma los
# comandos en Python: primero "display ip interface brief" para
# descubrir qué Vlanif existen, después "display current-configuration
# interface Vlanif{id}" (confirmado que esta forma con número sí
# funciona) por cada una -- mismo criterio de bypass de YAML que
# set_svi_dhcp_relay (número de comandos dinámico).
