---
- name: Delete VLAN on Huawei VRP device
  hosts: all
  gather_facts: false
  tasks:
    - name: Delete VLAN {{ vlan_id }}
      community.network.ce_command:
        commands:
          - system-view
          - undo vlan {{ vlan_id }}
          - return
