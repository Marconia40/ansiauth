---
- name: Create VLAN on Huawei VRP device
  hosts: all
  gather_facts: no

  tasks:
    - name: Configure VLAN with description
      ansible.netcommon.cli_command:
        command: |
          system-view
          vlan {{ vlan_id }}
          description {{ vlan_name }}
          quit
          commit
          quit
