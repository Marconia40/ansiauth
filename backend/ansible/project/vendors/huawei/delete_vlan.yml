---
- name: Delete VLAN on Huawei VRP device
  hosts: all
  gather_facts: no

  tasks:
    - name: Remove VLAN
      ansible.netcommon.cli_command:
        command: |
          system-view
          undo vlan {{ vlan_id }}
          commit
          quit
