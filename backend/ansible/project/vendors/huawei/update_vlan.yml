---
- name: Update VLAN description on Huawei VRP device
  hosts: all
  gather_facts: no

  tasks:
    - name: Update VLAN description
      ansible.netcommon.cli_command:
        command: |
          system-view
          vlan {{ vlan_id }}
          description {{ description }}
          quit
          commit
          quit
