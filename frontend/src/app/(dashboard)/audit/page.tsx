import { PageHeader } from '@/components/PageHeader';

export default function AuditPage() {
  return (
    <div>
      <PageHeader title="Audit Log" />
      <p className="text-gray-500 text-sm">Audit log viewer — coming in step 3.</p>
    </div>
  );
}
