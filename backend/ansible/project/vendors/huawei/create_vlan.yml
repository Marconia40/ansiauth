---
- name: Create VLAN on Huawei VRP device
  hosts: all
  gather_facts: no

  tasks:
    - name: Ensure VLAN exists with name
      community.network.ce_vlan:
        vlan_id: "{{ vlan_id }}"
        name: "{{ vlan_name }}"
        state: present
