MSP MIGRATION — PHASE 0 T0.4 TEST BASELINE
============================================

Purpose
-------
Record the current pass/fail counts of `backend/tests/` before any MSP code
change lands. Every subsequent phase's `pytest` output is compared against
this file: new failures = regression; new passes = new MSP tests landing.

Environment
-----------
Branch:     refactor/MSP-structure
HEAD:       4aad1d7  (Merge PR #7 refactor/celery-redis)
Runner:     local, python3 -m pytest tests/ -q  (from backend/)
DB backend: SQLite (dev DB at backend/app/db/app.db, not shared with tests)
Runtime:    ~185 seconds

Summary
-------
    943 passed
      3 failed        ← pre-existing, unrelated to MSP (see below)
      1 warning       ← DeprecationWarning on datetime.utcnow() in one test
    ────────────────
    946 collected total

Pre-existing failures (NOT MSP-related — do not block Phase 1)
--------------------------------------------------------------
All three failures are in `backend/tests/test_bootstrap.py`. They query
`UserModel` filtering by `role="admin"`, but the current `backend/app/main.py`
bootstrap seeds the initial user with `role="super-admin"` (see
`main.py:101`). The test file was not updated when the bootstrap default
was changed (commit `d6358a4 Change in start config first user`).

Failing tests:
  1. test_bootstrap.py::test_bootstrap_creates_admin_on_empty_db
  2. test_bootstrap.py::test_bootstrap_idempotent_skips_if_admin_exists
  3. test_bootstrap.py::test_bootstrap_uses_custom_username

Root cause (identical in all three):
     assert row is not None
     E   assert None is not None
     tests/test_bootstrap.py:71 (and analogous lines in the other two)
     session.query(UserModel).filter_by(role="admin").first()  ← returns None
                                             ^^^^^^^^^^^^
                                             now super-admin

Recommended handling
--------------------
Option A (do nothing): leave as pre-existing tech debt; note in phase reports.
Option B (fix now): update test_bootstrap.py to query
         `filter_by(role="super-admin")` — one-line change per assertion.
         Not MSP work, but painless to include in the Phase 1 PR.
Option C (defer): fix as part of Phase 2 Step 6a rewrites since those tests
         will have to be rewritten anyway once `role` becomes `is_system_admin`.

Regression gate for subsequent phases
-------------------------------------
Every phase-N PR must satisfy:

    (phase_N passed count) - (phase_N failed count)
        >=  943 - 3  =  940

If a phase reports fewer than 940 net passes without an explanation in the
`CHANGELOG.md` "Removed" section, block the merge and investigate.

New MSP tests introduced by each phase are additive to this baseline:
    Phase 1: +3 (T1.1, T1.2, T1.3 tests)
    Phase 2: +8 (all under backend/tests/migrations/)
    Phase 3: +≈12 (effective_role + scoped-list + grants + move + snapshot-diff)
    Phase 4: +≈8 (M3 + schema breaking + audit scoping)
    Phase 5: +1 (M4 drop)
    Phase 6: +≈4 (RLS + optional serial_number)

Warning captured for follow-up
------------------------------
    tests/test_token_lifecycle.py::test_expired_refresh_token_returns_401
        DeprecationWarning: datetime.utcnow() → use datetime.now(datetime.UTC)

Not MSP-related; suggest fixing in a general-cleanup PR at some later point.

Reproduce
---------
    cd backend
    python3 -m pytest tests/ -q --tb=no

For Docker workflow (recommended in CI):
    docker compose exec backend pytest backend/tests/ -q
