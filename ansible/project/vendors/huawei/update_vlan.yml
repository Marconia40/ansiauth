---
- name: Update VLAN name on Huawei VRP device
  hosts: all
  gather_facts: false
  tasks:
    - name: Update VLAN {{ vlan_id }} name
      community.network.ce_command:
        commands:
          - system-view
          - vlan {{ vlan_id }}
          - name {{ description }}
          - return
